// RU: Файл входит в проверенный контур первой волны.
import {RequestWorkbench} from "@/components/requests/request-workbench";

export default function RequestWorkbenchPage() {
  return <RequestWorkbench />;
}
